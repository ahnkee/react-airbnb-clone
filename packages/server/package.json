{
  "name": "@rac/server",
  "version": "1.0.0",
  "main": "app",
  "license": "MIT",
  "engines": {
    "node": ">=8.11.2"
  },
  "scripts": {
    "sequelize": "babel-node node_modules/.bin/sequelize",
    "migration:generate": "sequelize migration:generate --name",
    "seed:generate": "sequelize seed:generate --name",
    "start": "nodemon --exec 'node -r dotenv/config' app/index.js --exec babel-node"
  },
  "dependencies": {
    "@koa/cors": "2",
    "@rac/tools": "^1.0.0",
    "dotenv": "^6.0.0",
    "koa": "^2.5.1",
    "koa-bodyparser": "^4.2.1",
    "koa-helmet": "^4.0.0",
    "koa-morgan": "^1.0.1",
    "koa-router": "^7.4.0",
    "pg": "^7.4.3",
    "pg-hstore": "^2.3.2",
    "sequelize": "^4.37.10",
    "sequelize-cli": "^4.0.0",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "nodemon": "^1.17.5"
  }
}
